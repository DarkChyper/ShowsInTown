<nav class="navbar navbar-expand-lg navbar-light bg-light nav-pills nav-fill">
    <a class="navbar-brand" href="{{ path("homepage") }}">
        <img src="{{  asset('assets/img/logo.png') }}"  height="80" alt="{{ 'menu.logo.alt' | trans }}" loading="lazy">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerMenu"
            aria-controls="navbarTogglerMenu" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerMenu">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item {%  if current_page is defined and current_page  == 'homepage' %} active {% endif %}">
                <a class="nav-link" href="{{ path("homepage") }}">{{ 'menu.home' | trans }}</a>
            </li>
            <li class="nav-item {%  if current_page is defined and current_page  == 'dashboard' %} active {% endif %}">
                <a class="nav-link" href="{{ path("dashboard") }}">{{ 'menu.dashboard' | trans }}</a>
            </li>
        </ul>
        {% if current_page is defined %}
            <div>
                <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle no-background" href="#" id="chooseLocale" role="button" data-toggle="dropdown"
                           aria-haspopup="true" aria-expanded="false">
                            <span>{{ app.request.locale }}</span>
                        </a>
                        <div class="dropdown-menu resized" aria-labelledby="chooseLocale">
                            <ul class="navbar-nav mr-auto">
                                {% include '_locale_switcher.html.twig' %}
                            </ul>
                        </div>
                    </li>
                </ul>
            </div>
        {% endif %}
    </div>
</nav>